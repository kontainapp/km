# placeholder for misc script used when debugging python.km and need some payload.
# VSCode launch.json defines this a default for python.km debug
# place the code you want here, it is only used as a helper to feed python commands when debugging

import numpy
print(dir(numpy))
